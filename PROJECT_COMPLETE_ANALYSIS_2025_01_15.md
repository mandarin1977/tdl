# TDL 프로젝트 전체 파일 분석 보고서

**생성일**: 2025-01-15  
**프로젝트명**: TDL (NFT 게임 플랫폼)  
**프레임워크**: Vue 3 + Vite  
**분석 범위**: 전체 프로젝트 파일

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [기술 스택 및 의존성](#기술-스택-및-의존성)
3. [프로젝트 구조 상세 분석](#프로젝트-구조-상세-분석)
4. [주요 파일 분석](#주요-파일-분석)
5. [데이터 흐름 및 상태 관리](#데이터-흐름-및-상태-관리)
6. [인증 시스템 분석](#인증-시스템-분석)
7. [게임 시스템 분석](#게임-시스템-분석)
8. [코드 품질 및 개선 사항](#코드-품질-및-개선-사항)
9. [보안 분석](#보안-분석)
10. [성능 최적화 권장사항](#성능-최적화-권장사항)

---

## 프로젝트 개요

TDL은 NFT 기반의 클리커 게임 플랫폼입니다. 사용자는 고양이 NFT를 수집하고, 다양한 게임 모드에서 NFT를 활용하여 포인트와 코인을 획득할 수 있습니다.

### 핵심 특징
- 🎮 4가지 게임 모드 (채굴, 사냥, 탐험, 생산)
- 🐱 NFT 고양이 수집 및 관리 (30가지 이미지)
- 💰 포인트(P)와 코인(C) 이중 화폐 시스템
- 🔌 Web3 지갑 연결 지원 (MetaMask, Ethers.js v6)
- 🔥 Firebase 백엔드 연동 (Auth, Firestore, Analytics)
- 🌐 다국어 지원 (한국어/영어)
- 📱 모바일 우선 반응형 디자인

---

## 기술 스택 및 의존성

### 프론트엔드 프레임워크
```json
{
  "vue": "^3.5.22",           // Vue 3 Composition API
  "vue-router": "^4.6.0",     // 라우팅
  "vite": "^7.1.7"            // 빌드 도구
}
```

### Web3 통합
```json
{
  "ethers": "^6.15.0"         // Web3 지갑 연결 (MetaMask)
}
```

### 백엔드/데이터베이스
```json
{
  "firebase": "^12.5.0"        // Firebase SDK
}
```
- **Firebase Auth**: Google 로그인, 이메일/비밀번호 인증
- **Firestore**: 사용자 데이터 및 게임 데이터 저장
- **Analytics**: 사용자 분석

### 개발 도구
```json
{
  "@vitejs/plugin-vue": "^6.0.1",
  "vite-plugin-vue-devtools": "^8.0.2",
  "gh-pages": "^6.3.0"         // GitHub Pages 배포
}
```

---

## 프로젝트 구조 상세 분석

```
tdl/
├── src/
│   ├── assets/              # 정적 파일
│   │   ├── audio/          # BGM, 효과음
│   │   └── img/            # 게임 이미지 (68개 PNG 파일)
│   │       ├── tabIcon*.png
│   │       ├── statStar*.png
│   │       ├── inventoryTab_*.png
│   │       └── ...
│   │
│   ├── components/         # 공통 컴포넌트
│   │   ├── Header.vue     # 상단 헤더 (리소스 표시)
│   │   └── Footer.vue     # 하단 네비게이션 바
│   │
│   ├── firebase/          # Firebase 설정
│   │   └── config.js      # Firebase 초기화 및 설정 (407줄)
│   │
│   ├── pages/             # 페이지 컴포넌트 (19개)
│   │   ├── LoadingScreen.vue
│   │   ├── LoginScreen.vue      # 로그인 (745줄)
│   │   ├── SignupScreen.vue
│   │   ├── MainScreen.vue       # 메인 게임 화면 (약 2900줄)
│   │   ├── HomePage.vue
│   │   ├── MiningPage.vue        # 채굴 페이지
│   │   ├── HuntingPage.vue       # 사냥 페이지
│   │   ├── ExplorationPage.vue  # 탐험 페이지
│   │   ├── ProductionPage.vue   # 생산 페이지
│   │   ├── ExchangePage.vue     # 교환소
│   │   ├── QuestPage.vue        # 퀘스트
│   │   ├── InventoryPage.vue    # 인벤토리 (1878줄)
│   │   ├── FactoryPage.vue      # 제작소
│   │   ├── SettingsPage.vue     # 설정
│   │   ├── UserProfilePage.vue  # 프로필
│   │   ├── AttendancePage.vue   # 출석체크
│   │   ├── NFTPage.vue          # NFT 마켓플레이스
│   │   ├── NotificationPage.vue # 알림
│   │   └── ShopPage.vue         # 상점
│   │
│   ├── store/             # 상태 관리
│   │   ├── appStore.js    # 전역 상태 관리 (369줄)
│   │   └── example-usage.vue
│   │
│   ├── styles/            # 전역 스타일
│   │   ├── common.css
│   │   ├── layout.css
│   │   └── main.css
│   │
│   ├── utils/             # 유틸리티 함수 (13개)
│   │   ├── userUtils.js         # 사용자 데이터 관리 (889줄)
│   │   ├── wallet.js            # 지갑 연결 (251줄)
│   │   ├── firebaseAuth.js      # Firebase 인증 (379줄)
│   │   ├── energyUtils.js       # 에너지 관리
│   │   ├── formatUtils.js       # 숫자 포맷팅
│   │   ├── nftLevelUtils.js    # NFT 레벨 관리
│   │   ├── rarityUtils.js       # 레어리티 계산
│   │   ├── questUtils.js        # 퀘스트 관리
│   │   ├── marketplaceUtils.js # 마켓플레이스
│   │   ├── referralUtils.js     # 추천인 시스템
│   │   ├── kakaoUtils.js        # 카카오톡 공유
│   │   ├── passwordUtils.js     # 비밀번호 유틸리티
│   │   └── createTestAccount.js # 테스트 계정 생성
│   │
│   ├── App.vue            # 루트 컴포넌트 (141줄)
│   └── main.js            # 진입점 (169줄)
│
├── public/                # 정적 파일
│   └── favicon.ico
│
├── .vscode/              # VS Code 설정
│   └── extensions.json
│
├── package.json          # 의존성 관리
├── vite.config.js        # Vite 설정 (18줄)
├── jsconfig.json         # JavaScript 설정
├── index.html            # HTML 진입점
└── README.md             # 프로젝트 문서
```

---

## 주요 파일 분석

### 1. main.js (169줄)

**역할**: 애플리케이션 진입점 및 라우터 설정

**주요 기능**:
- Vue 앱 초기화
- Vue Router 설정 (Hash 모드)
- 19개 라우트 정의
- 라우터 가드 구현 (인증 체크)
- Firebase 사용자 → 앱 사용자 변환

**라우트 구조**:
```javascript
// 공개 라우트
- /loading → LoadingScreen
- /login → LoginScreen
- /signup → SignupScreen

// 보호된 라우트
- /main → MainScreen (게임 화면)
- /home → HomePage
- /mining → MiningPage
- /hunting → HuntingPage
- /exploration → ExplorationPage
- /production → ProductionPage
- /exchange → ExchangePage
- /quest → QuestPage
- /inventory → InventoryPage
- /factory → FactoryPage
- /settings → SettingsPage
- /profile → UserProfilePage
- /attendance → AttendancePage
- /nft → NFTPage
- /notification → NotificationPage
- /shop → ShopPage
```

**라우터 가드 로직**:
1. sessionStorage에서 현재 사용자 확인
2. Firebase 인증 상태 확인 (Google 로그인 리다이렉트 처리)
3. 인증되지 않은 사용자는 `/login`으로 리다이렉트
4. 로그인된 사용자가 로그인/회원가입 페이지 접근 시 `/home`으로 리다이렉트

**특이사항**:
- Google 로그인 리다이렉트 후 인증 상태 복구 로직 포함
- 100ms 대기 후 재확인 로직 (타이밍 이슈 대응)

---

### 2. App.vue (141줄)

**역할**: 루트 컴포넌트, 전역 설정

**주요 기능**:
- Google 로그인 리다이렉트 처리 (최우선)
- 사용자 데이터 로드
- 지갑 연결 상태 확인 및 리스너 설정
- BGM 재생 제어
- 소리 설정 변경 감지

**BGM 시스템**:
- `@/assets/audio/bgm.mp3` 재생
- 사용자 상호작용 후 자동 재생
- 소리 설정에 따른 재생/일시정지 제어
- `soundSettingChanged` 커스텀 이벤트 리스너

---

### 3. store/appStore.js (369줄)

**역할**: 전역 상태 관리 (Pinia 스타일 커스텀 스토어)

**상태 구조**:
```javascript
state = {
  // 인증 상태
  isLoggedIn: boolean
  loginType: 'email' | 'wallet' | null
  walletAddress: string
  userBalance: string
  isWalletConnected: boolean
  walletProvider: Provider | null
  isLoading: boolean
  
  // 사용자 정보
  currentUser: User | null
  
  // 게임 데이터
  coins: number              // 포인트 (P)
  totalCoin: number          // 코인 (C)
  catFragments: number
  level: number
  nftCount: number
  
  // 모드별 레벨
  miningLevel: number
  huntingLevel: number
  explorationLevel: number
  productionLevel: number
  
  // NFT 슬롯 (각 모드당 4개)
  miningCats: Array<Cat | null>
  huntingCats: Array<Cat | null>
  explorationCats: Array<Cat | null>
  productionCats: Array<Cat | null>
  
  // 인벤토리
  inventory: Cat[]
}
```

**주요 액션**:
- `loadCurrentUser()`: sessionStorage에서 사용자 로드
- `syncGameData()`: currentUser.gameData → state 동기화
- `updateGameData()`: Firebase/localStorage + state 업데이트
- `connectWalletToApp()`: MetaMask 연결
- `disconnectWalletFromApp()`: 지갑 연결 해제
- `checkWalletStatus()`: 지갑 연결 상태 확인
- `setupWalletListeners()`: 지갑 이벤트 리스너 설정

**데이터 동기화 흐름**:
1. localStorage ↔ state: `loadCurrentUser()` 호출 시
2. Firebase ↔ localStorage: `updateUserGameData()` 호출 시
3. state ↔ 컴포넌트: 반응형 computed 속성 사용
4. 커스텀 이벤트: `userDataUpdated` 이벤트 발생

**특이사항**:
- 읽기 전용 Proxy로 직접 상태 수정 방지
- 4개 슬롯 제한 로직 (`ensure4Slots`)

---

### 4. firebase/config.js (407줄)

**역할**: Firebase 초기화 및 설정

**주요 기능**:
- Firebase 앱 초기화 (중복 방지)
- Auth 및 Firestore 인스턴스 생성
- Analytics 초기화
- Google 로그인 제공자 설정
- 이메일/비밀번호 인증 함수
- Firestore CRUD 함수

**환경 변수**:
```javascript
VITE_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID
VITE_FIREBASE_MEASUREMENT_ID
```

**주요 함수**:
- `signInWithGoogle()`: Google 로그인 (리다이렉트 방식)
- `handleRedirectResult()`: 리다이렉트 결과 처리
- `signUpWithEmail()`: 이메일 회원가입
- `signInWithEmail()`: 이메일 로그인
- `getUserGameData()`: Firestore에서 게임 데이터 조회
- `updateUserGameDataInFirestore()`: Firestore에 게임 데이터 업데이트
- `saveUserToFirestore()`: 새 사용자 Firestore 저장

**에러 처리**:
- 설정 유효성 검사 (`validateFirebaseConfig`)
- 중복 초기화 방지
- 개발 환경 디버깅 로그

---

### 5. utils/firebaseAuth.js (379줄)

**역할**: Firebase 인증 유틸리티

**주요 기능**:
- Google 로그인 처리
- 리다이렉트 결과 처리 (중복 방지)
- Firebase 사용자 → 앱 사용자 변환
- 인증 상태 변경 리스너
- 게임 데이터 업데이트

**리다이렉트 처리 로직**:
1. 중복 호출 방지 (`googleRedirectHandled` 플래그)
2. Promise 캐싱 (`redirectProcessingPromise`)
3. Firebase 인증 상태 즉시 확인
4. `handleRedirectResult()` 호출
5. 재시도 로직 (10회, 200ms 간격)
6. `onAuthStateChanged` 대기 (최대 10초)

**특이사항**:
- 복잡한 리다이렉트 처리 로직 (타이밍 이슈 대응)
- 세션 복구 로직 포함
- 테스트용 플래그 리셋 함수 제공

---

### 6. utils/userUtils.js (889줄)

**역할**: 사용자 데이터 관리 및 다국어 지원

**주요 기능**:
- 사용자 CRUD (localStorage 기반)
- 지갑 사용자 생성
- 게임 데이터 업데이트
- 다국어 텍스트 관리 (한국어/영어)

**다국어 시스템**:
- `getCurrentLanguage()`: 현재 언어 반환 (기본값: English)
- `getI18nTexts()`: 언어별 텍스트 객체 반환
- 887개 텍스트 키 (한국어/영어)

**사용자 데이터 구조**:
```javascript
{
  id: string                    // 이메일 또는 지갑 주소
  email?: string                // 이메일 (이메일 로그인 시)
  walletAddress?: string        // 지갑 주소 (지갑 로그인 시)
  loginType: 'email' | 'wallet'
  inviteCode: string            // 추천인 코드
  createdAt: string
  referrals: string[]           // 추천한 사용자 ID 배열
  gameData: {
    level: number
    coins: number               // 포인트 (P)
    totalCoin: number            // 코인 (C)
    catFragments: number
    nftCount: number
    miningLevel: number
    huntingLevel: number
    explorationLevel: number
    productionLevel: number
    miningCats: Array<Cat | null>
    huntingCats: Array<Cat | null>
    explorationCats: Array<Cat | null>
    productionCats: Array<Cat | null>
    inventory: Cat[]
  }
}
```

---

### 7. utils/wallet.js (251줄)

**역할**: Web3 지갑 연결 (MetaMask)

**주요 기능**:
- MetaMask 설치 확인
- 지갑 연결 (`eth_requestAccounts`)
- 지갑 주소/잔액 조회
- 네트워크 정보 조회
- 네트워크 전환
- 계정 변경 감지 (`accountsChanged`)
- 네트워크 변경 감지 (`chainChanged`)

**사용 라이브러리**:
- `ethers.js` v6 (`BrowserProvider`)

**주요 함수**:
- `connectWallet()`: 지갑 연결
- `getWalletAddress()`: 현재 연결된 주소 조회
- `getWalletBalance()`: 지갑 잔액 조회
- `checkWalletConnection()`: 연결 상태 확인 (팝업 없음)
- `onAccountsChanged()`: 계정 변경 리스너
- `onChainChanged()`: 네트워크 변경 리스너

---

### 8. components/Header.vue (377줄)

**역할**: 상단 헤더 컴포넌트

**주요 기능**:
- 리소스 표시 (에너지, 포인트, 코인, 고양이 파편)
- NFT, 설정, 알림 아이콘 버튼
- 실시간 데이터 업데이트 (500ms 간격)
- 스토어 연동 (반응형)

**리소스 표시**:
- 에너지: localStorage 기반
- 포인트: `store.state.coins`
- 코인: `store.state.totalCoin`
- 고양이 파편: `store.state.catFragments`

**이벤트 처리**:
- `userDataUpdated` 커스텀 이벤트 리스너
- 에너지 체크 및 리셋 (`checkAndResetEnergy`)

---

### 9. components/Footer.vue (221줄)

**역할**: 하단 네비게이션 바

**주요 기능**:
- 5개 탭 (게임, 교환소, 홈, 인벤토리, 제작소)
- 현재 경로에 따른 활성 탭 표시
- 다국어 지원

**탭 구조**:
- 게임 (`/main`)
- 교환소 (`/exchange`) - 클릭 시 이동 없음
- 홈 (`/home`)
- 인벤토리 (`/inventory`)
- 제작소 (`/factory`)

---

### 10. pages/LoginScreen.vue (745줄)

**역할**: 로그인 화면

**주요 기능**:
- 이메일/비밀번호 로그인
- Google 로그인
- 지갑 연결 로그인
- 비밀번호 표시/숨김
- 다국어 지원

**인증 방식**:
1. **이메일/비밀번호**: localStorage 기반 (해시화된 비밀번호)
2. **Google 로그인**: Firebase Auth (리다이렉트 방식)
3. **지갑 연결**: MetaMask (Ethers.js)

**비밀번호 보안**:
- `passwordUtils.js` 사용 (해시화)
- 기존 평문 비밀번호 마이그레이션 지원

---

### 11. pages/InventoryPage.vue (1878줄)

**역할**: 인벤토리 페이지

**주요 기능**:
- NFT 목록 표시
- 필터링 (별 등급, 레벨, 레어리티)
- 정렬 (최신순, 오래된순, 레벨순 등)
- 검색 (이름 또는 ID)
- NFT 상세 정보 모달
- NFT 레벨업
- 스탯 표시 (6가지: 근력, 체력, 지능, 손재주, 용기, 행운)

**필터/정렬 옵션**:
- 별 등급: 전체, 2, 3, 4, 5
- 레벨: 전체, 1-5, 6-10, 11+
- 레어리티: 전체, Common, Rare, Epic, Legendary
- 정렬: 최신순, 오래된순, 레벨순, 이름순, 별순, 레어리티순

---

## 데이터 흐름 및 상태 관리

### 데이터 저장소

1. **localStorage**
   - `users`: 모든 사용자 데이터 배열
   - `currentEnergy`: 현재 에너지
   - `energyLastDate`: 에너지 마지막 업데이트 날짜
   - `soundEnabled`: 소리 설정
   - `appLanguage`: 언어 설정

2. **sessionStorage**
   - `currentUser`: 현재 로그인한 사용자 정보

3. **Firestore (Firebase)**
   - `users/{userId}`: 사용자 문서
     - `email`, `displayName`, `photoURL`
     - `gameData`: 게임 데이터 객체

### 데이터 동기화 흐름

```
[사용자 액션]
    ↓
[컴포넌트]
    ↓
[appStore.updateGameData()]
    ↓
[userUtils.updateUserGameData()]
    ├─→ localStorage 업데이트
    └─→ Firebase 업데이트 (Firestore)
    ↓
[state 업데이트]
    ↓
[커스텀 이벤트 발생: 'userDataUpdated']
    ↓
[모든 컴포넌트 자동 업데이트]
```

---

## 인증 시스템 분석

### 인증 방식

1. **이메일/비밀번호**
   - localStorage 기반
   - 비밀번호 해시화 (`passwordUtils.js`)
   - Firebase Auth 연동 가능

2. **Google 로그인**
   - Firebase Auth (리다이렉트 방식)
   - 복잡한 리다이렉트 처리 로직
   - 세션 복구 기능

3. **지갑 연결**
   - MetaMask (Ethers.js v6)
   - 지갑 주소 기반 사용자 생성
   - 계정 변경 감지

### 인증 흐름

**Google 로그인**:
```
[사용자 클릭] → [signInWithRedirect] → [Google 리다이렉트]
    ↓
[리다이렉트 복귀] → [handleGoogleRedirect]
    ├─→ [handleRedirectResult] 또는
    ├─→ [auth.currentUser 확인] 또는
    └─→ [onAuthStateChanged 대기]
    ↓
[Firebase User → App User 변환]
    ↓
[sessionStorage 저장]
    ↓
[appStore.loadCurrentUser()]
```

**지갑 연결**:
```
[사용자 클릭] → [connectWallet]
    ↓
[MetaMask 팝업] → [계정 선택]
    ↓
[지갑 주소 조회] → [잔액 조회]
    ↓
[사용자 생성/로드] → [sessionStorage 저장]
    ↓
[appStore.loadCurrentUser()]
```

---

## 게임 시스템 분석

### 게임 모드

1. **채굴 (Mining)**
   - 클릭 기반 채굴
   - 10번 클릭마다 포인트 획득
   - 레벨에 따른 포인트 배수
   - NFT 효율 보너스 적용

2. **사냥 (Hunting)**
   - 몬스터 처치 시스템
   - 데미지 계산 및 경험치 획득
   - NFT 데미지 보너스

3. **탐험 (Exploration)**
   - 지역 탐험 시스템
   - 보물 및 자원 획득
   - NFT 탐험 보상 보너스

4. **생산 (Production)**
   - 아이템 제작 시스템
   - 레시피 기반 생산
   - NFT 생산 속도 보너스

### NFT 시스템

**NFT 속성**:
- 레벨 (Level)
- 별 등급 (Stars: 1-5)
- 레어리티 (Common, Rare, Epic, Legendary)
- 게임 모드별 효율:
  - `miningEfficiency`: 채굴 효율 (%)
  - `huntingDamage`: 사냥 데미지 (%)
  - `explorationReward`: 탐험 보상 (%)
  - `productionSpeed`: 생산 속도 (%)
- 스탯 (6가지):
  - 근력 (Strength)
  - 체력 (Health)
  - 지능 (Intelligence)
  - 손재주 (Dexterity)
  - 용기 (Courage)
  - 행운 (Luck)
- 성격 (Personality)
- 친밀도 (Affinity: 30-100%)

**NFT 슬롯**:
- 각 모드당 최대 4개 NFT 배치 가능
- 자동 포인트 획득 (1초마다)

### 화폐 시스템

- **포인트 (P)**: 기본 화폐, 클릭으로 획득
- **코인 (C)**: 고급 화폐, 교환소에서 거래
- **고양이 파편**: NFT 제작에 사용

### 에너지 시스템

- 하루 최대 4000 에너지
- 클릭당 1 에너지 소모
- 자정 자동 회복
- 행성 에너지: 10번 클릭마다 50씩 증가, 2000 도달 시 레벨업

---

## 코드 품질 및 개선 사항

### 긍정적인 점

1. ✅ **구조화된 프로젝트**: 명확한 폴더 구조
2. ✅ **모듈화**: 유틸리티 함수 분리
3. ✅ **다국어 지원**: 한국어/영어 지원
4. ✅ **반응형 디자인**: 모바일 우선 설계
5. ✅ **에러 처리**: 주요 함수에 에러 처리 포함

### 개선이 필요한 부분

#### 1. 코드 구조

**문제점**:
- `MainScreen.vue`: 약 2900줄로 너무 큼
- `InventoryPage.vue`: 1878줄
- 단일 파일에 너무 많은 로직 집중

**권장사항**:
```javascript
// MainScreen.vue 분리
- GameModeTabs.vue      // 모드 탭 전환
- ClickerArea.vue        // 클리커 영역
- CatSlots.vue           // NFT 슬롯
- CatSelectPopup.vue     // 고양이 선택 팝업
- AutoPointDisplay.vue    // 자동 포인트 표시
```

#### 2. 상태 관리

**현재**: 커스텀 상태 관리 (Pinia 스타일)

**권장**: Pinia 도입
- 더 나은 타입 지원
- DevTools 통합
- 모듈화된 스토어 구조
- 테스트 용이성

#### 3. 타입 안정성

**현재**: JavaScript만 사용

**권장**: TypeScript 도입
- 타입 안정성 향상
- IDE 자동완성 개선
- 런타임 에러 감소

#### 4. 중복 코드

**문제점**:
- Google 로그인 리다이렉트 처리 로직이 여러 곳에 분산
- 사용자 데이터 변환 로직 중복

**권장사항**:
- 공통 로직을 유틸리티 함수로 추출
- 단일 책임 원칙 적용

#### 5. 에러 처리

**개선 필요**:
- 전역 에러 핸들러 추가
- 사용자 친화적인 에러 메시지
- 에러 로깅 시스템 (Sentry 등)

#### 6. 테스트 코드

**현재**: 테스트 코드 없음

**권장**:
- Unit 테스트 (Vitest)
- E2E 테스트 (Playwright)
- 컴포넌트 테스트 (Vue Test Utils)

---

## 보안 분석

### 현재 보안 조치

1. ✅ **비밀번호 해시화**: `passwordUtils.js` 사용
2. ✅ **환경 변수**: Firebase 설정을 환경 변수로 관리
3. ✅ **라우터 가드**: 인증되지 않은 접근 차단

### 보안 개선 권장사항

1. **입력값 검증**
   - 모든 사용자 입력 검증
   - XSS 방지 (DOMPurify 등)
   - SQL Injection 방지 (Firestore는 자동 방지)

2. **CSRF 보호**
   - Firebase Auth는 자동 CSRF 보호
   - 추가 보호 필요 시 토큰 사용

3. **환경 변수 검증**
   - 앱 시작 시 환경 변수 유효성 검사
   - 누락된 변수에 대한 명확한 에러 메시지

4. **세션 관리**
   - 세션 타임아웃 설정
   - 자동 로그아웃 기능

5. **Firestore 보안 규칙**
   - 사용자별 데이터 접근 제한
   - 쓰기 권한 제한

---

## 성능 최적화 권장사항

### 현재 성능 이슈

1. **이미지 최적화**
   - 68개 PNG 파일
   - WebP 형식으로 변환 권장
   - 이미지 지연 로딩 (lazy loading)

2. **코드 스플리팅**
   - 현재: 모든 페이지가 번들에 포함
   - 권장: 라우트별 코드 스플리팅

3. **불필요한 리렌더링**
   - `Header.vue`: 500ms 간격 폴링
   - 권장: 이벤트 기반 업데이트로 변경

4. **메모이제이션**
   - 계산 비용이 큰 computed 속성에 메모이제이션 적용
   - `v-memo` 디렉티브 활용

5. **가상 스크롤**
   - 인벤토리 목록이 많을 경우 가상 스크롤 적용

### 최적화 예시

```javascript
// 코드 스플리팅
const MainScreen = () => import('./pages/MainScreen.vue')
const InventoryPage = () => import('./pages/InventoryPage.vue')

// 이미지 지연 로딩
<img v-lazy="imageSrc" alt="NFT" />

// 메모이제이션
const expensiveComputed = computed(() => {
  // 복잡한 계산
}).memo()
```

---

## 결론

TDL 프로젝트는 Vue 3 기반의 잘 구조화된 NFT 게임 플랫폼입니다. 주요 기능들이 구현되어 있으며, Firebase와 Web3 지갑 연동도 포함되어 있습니다.

### 강점
- ✅ 명확한 프로젝트 구조
- ✅ 다국어 지원
- ✅ Web3 통합
- ✅ Firebase 백엔드 연동
- ✅ 모바일 우선 반응형 디자인

### 개선 필요
- ⚠️ 코드 구조 개선 (대형 컴포넌트 분리)
- ⚠️ 타입 안정성 강화 (TypeScript 도입)
- ⚠️ 테스트 코드 추가
- ⚠️ 성능 최적화 (이미지, 코드 스플리팅)
- ⚠️ 에러 처리 강화

### 우선순위별 개선 계획

**높은 우선순위**:
1. MainScreen.vue 분리 (가독성 및 유지보수성)
2. Pinia 도입 (상태 관리 개선)
3. 에러 처리 강화 (사용자 경험)

**중간 우선순위**:
4. TypeScript 도입 (타입 안정성)
5. 테스트 코드 추가 (품질 보증)
6. 이미지 최적화 (성능)

**낮은 우선순위**:
7. 코드 스플리팅 (초기 로딩 속도)
8. 가상 스크롤 (대량 데이터 처리)

---

**분석 완료일**: 2025-01-15  
**분석자**: AI Assistant  
**프로젝트 버전**: 0.0.0  
**총 파일 수**: 약 100개 이상  
**총 코드 라인 수**: 약 10,000줄 이상
